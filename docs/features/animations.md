# アニメーション・ビジュアル強化

**カテゴリ**: UX改善 | **優先度**: 任意

UIの視覚的な洗練とマイクロインタラクションを実装する。

---

## 目的

- スムーズなアニメーション
- 視覚的フィードバックの強化

---

## テスト要件

アニメーションは視覚的確認が主のため、ユニットテストは最小限。

### 手動確認項目

| 項目 | 確認内容 |
|------|---------|
| slideUp | トーストが下から上にスライドインする |
| fadeIn | 要素がフェードインする |
| spin | スピナーが回転する |
| progressStripe | プログレスバーにストライプアニメーションがある |

---

## 実装内容

### 1. アニメーション定義

`src/styles/animations.css` に以下のアニメーションを定義：

| アニメーション | 説明 | 用途 |
|--------------|------|------|
| slideUp | 下から上にスライドイン | トースト表示 |
| fadeIn | フェードイン | 要素の表示 |
| spin | 360度回転 | ローディングスピナー |
| progressStripe | 斜めストライプの移動 | プログレスバー |

### 2. CSSクラス

| クラス | 説明 |
|-------|------|
| `.animate-slide-up` | スライドアップアニメーション（0.3s） |
| `.animate-fade-in` | フェードインアニメーション（0.2s） |
| `.animate-spin` | 回転アニメーション（1s infinite） |
| `.progress-stripe` | プログレスバーのストライプ効果 |

### 3. Tailwind設定更新

`tailwind.config.js` の `extend` に以下を追加：

- animation: `slide-up`, `fade-in`
- keyframes: 対応するキーフレーム定義

### 4. index.css 更新

- `@import './styles/animations.css'` を追加
- スクロールバーのカスタムスタイル

---

## 作成ファイル

| ファイル | 説明 |
|---------|------|
| `src/styles/animations.css` | アニメーション定義 |
| `tailwind.config.js` | 設定更新 |
| `src/index.css` | スタイル統合 |

---

## 完了条件

- [ ] トーストがスライドインする
- [ ] プログレスバーがストライプアニメーションする
- [ ] 読み込み中のスピナーが回転する
